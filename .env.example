# PostgreSQL接続文字列(実際の環境に合わせて設定してください)
DATABASE_URL="postgresql://user:password@localhost:5432/lounge_system?schema=public"

# NextAuth用のシークレット(openssl rand -base64 32 などで生成)
NEXTAUTH_SECRET="replace-with-a-random-secret"
NEXTAUTH_URL="http://localhost:3000"
